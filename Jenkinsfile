pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Build stage – no app yet'
            }
        }

        stage('Test') {
            steps {
                echo 'Test stage – placeholder'
            }
        }
    }
}
